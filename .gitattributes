# 统一按 LF 入库、并保持检出为 LF。
#
# 关键原因：services/ithqbot/entrypoint.sh 会被 COPY 进 Linux 镜像并作为
# ENTRYPOINT 执行。若在 Windows 上被检出成 CRLF，shebang 会变成
# "#!/bin/sh\r"，容器启动直接报 bad interpreter。
#
# Windows 默认 core.autocrlf=true，因此这里显式钉死，避免"我这儿好好的、
# 别人 clone 下来跑不起来"。
* text=auto eol=lf

# Dockerfile 与 shell 脚本必须是 LF
*.sh    text eol=lf
*.ps1   text eol=lf
Dockerfile  text eol=lf

# 二进制：不要做任何换行或编码转换
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.woff  binary
*.woff2 binary
*.ttf   binary
*.otf   binary
*.zip   binary
*.pdf   binary
